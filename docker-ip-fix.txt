#Shutdown and delete docker0 ip
sudo ip link set docker0 down
sudo ip link delete docker0

#Delete additional bridges, (in case that exists)
sudo ip link delete br-eaabda36e0db
sudo ip link delete br-fc670951b635

#Useful commands for debugging
ip rout
ip addr

#See virtualization.docker segment in configuration.nix to see
#how was fixed the issue, converting docker invisible to problematic
#ip's. Ex: UNA wifi portal or connections
